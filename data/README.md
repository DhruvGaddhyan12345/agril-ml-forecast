# Data

`raw/` contains generated observations and `processed/` contains feature matrices created by the pipeline. The generator is deterministic for a given seed and is designed for local testing, not agronomic decisions. Real deployments should replace it with governed farm, weather, soil, and yield sources.
